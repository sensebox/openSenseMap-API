version: "3.9"

services:
  db:
    build:
      context: ./images/mongodb
    container_name: osem-dev-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - api-db-network

volumes:
  mongo-data:
networks:
  api-db-network:
    external: true
